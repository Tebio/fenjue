# DeepSeek 红队审计裁决书（2026-09-19，付费 deepseek-chat，46,958 tokens）

流程：DS 挑刺 → K3 逐条复核（亲验代码/数据）→ 修复 → 复跑验证。DS 自述≠事实。

## 裁决总表

| # | 指控 | 裁决 | 证据/修复 |
|---|---|---|---|
| F1 | pick="deep" 排序方向反了 | **驳回**（方向正确） | key=c/ma-1 升序=最深在前；且容量实证全胜（若反了应输）。采纳其加固建议：脏数据防御已加 |
| F2 | ma60_out 双实现漂移 | **实锤** | exit_rule_grid 与 _ma60_exit_hold 口径细节不一；违反单实现纪律。登记为迁移项（fjcore 收编时单一化） |
| F3 | _XLDC 在幂等守卫前重置→重复调用清空 | **实锤（最危险）** | 已在守卫后初始化；影子盘二次加载场景会静默丢跌停横截面 |
| S1 | _fund_healthy O(n²) 线性扫描 | **实锤** | 改 packed(dates,vals)+二分；统一 fund_at() 入口 |
| S2 | 剔亏ST无数据=不健康=选择偏差 | **量化后降级** | 无 fund 数据仅 2.3%（<5% 阈值）；且无数据组 77.6%/+4.83 反而更好——剔除是保守方向，不构成「PASS 靠数据可得性」 |
| S3 | cross_matrix RNG 顺序依赖不可复现 | **实锤** | 每对独立 seed；重跑幸存对/判决全同 ✅ |
| S4 | Stage1 幸存线无 t 值/多重比较校正 | **部分采纳** | Stage2 有 Harvey t_NW≥3 兜底；Stage1 是纯漏斗。记录，FDR 留欠账 |
| S5 | IC 是伪秩（重复值覆盖） | **实锤** | 改真 Spearman 平均秩；重跑 IC 不变（+0.117）结论稳固 ✅ |
| S7 | 配对差用 NW-t 无意义 | **实锤** | 改配对 t（零依赖自实现）；10:30 优势 t=15.4 依旧极显著 ✅ |
| S8 | ex-div 哨兵跨缺口误标 | **实锤（轻微）** | 改相邻交易日才判定 |
| S9 | 一字跌停被误标「买不进」 | **实锤** | 跌停一字可成交；两处+rebuild 全改「一字涨停才买不进」。重建账本仅 3 单变化 |
| S10 | 高开涨停没判全天封死 | **实锤** | 加 amp<0.01 条件（全天封死才买不进） |
| M12 | R3 加仓腿出场日少一天 | **实锤** | 加仓腿改独立持有期 ei+1+hold |
| M17 | 回填只查最后 6 根 bar | **实锤** | 改二分全量查 |
| M18 | 影子桥静默吞异常 | **实锤** | KeyError/异常改打印警告 |
| M22 | _collect_sigs n-6 截断长窗 | **实锤** | 改 n-22，容量模拟长持有窗口不再截断 |
| M1-M11/M13-16/M20-26 | 轻微/记录 | 采纳 | 登记在案（口径统一、性能、边界） |

## 修复后的复跑验证
- pick_ranker：真 Spearman 下 超跌60 IC +0.117 不变 ✅（中间还抓到一个连带：S1 结构变更差点让 ranker 静默丢 96% 事件，已修——红队修复本身也会引入 bug，修完必须复跑）
- intraday_panic_grid：配对 t 下 10:30 优势不变（t=15.4/16.5）✅
- cross_matrix：固定 seed 后 9 幸存/8 PASS 完全复现 ✅
- shadow_rebuild v2：4098 单仅 3 单变化（一字方向修正影响极小，账本干净）✅
- 三连阴 PIT 修复后 submit：全七闸 PASS 不变 ✅

## 元教训（写给制度）
1. DS 的指控也要复核——F1 方向指控就是错的，照单全收会把对的改错
2. 红队修复本身会引入新 bug（S1 修复 → ranker 静默丢事件）——修复必须带复跑
3. 「同一逻辑两处实现」是本项目最大的 bug 温床（F2/S9/S10/M 系列反复出现）→ fjcore 门面收编是正解
