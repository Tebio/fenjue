# 自查报告：S1-S2 代码与逻辑漏洞猎杀（2026-09-12 凌晨，用户要求"别急着S3，先验证这轮的bug"）

> 方法：静态审查今晚全部新代码 → 列嫌疑点 → 逐个大样本实证，不靠推理结案。

## 判决汇总

| # | 嫌疑 | 严重度 | 实证结果 |
|---|---|---|---|
| B1 | update_kcache 增量接缝：前复权随查询终点漂移 → 缓存接缝断裂 | 疑似**致命** | **实证排除**：万科A 同一交易日（2019-06-03）以 2019 为锚 vs 以 2026 为锚查询，返回值逐位一致（20.7598419200）→ baostock 前复权锚定最新数据，增量 append 安全 ✅ |
| B2 | claims_audit 滚动窗口按**信号日**切 250 个，稀疏信号的"近期"会拉到数年 | 中 | **实锤 bug，已修**：改按交易日历切（全宇宙日期集倒数第250个交易日）。修正后重跑，三条形态主张 DECAYING→**DEAD**（见下） |
| B3 | law_pipeline ma60 构造多出一个尾部元素 | 微 | 实锤（len+1），无害（越界元素从不被读），已修 |
| B4 | update_m60 delta 只拉 16 根≈4 天，cron 漏跑 3 天就断档 | 微 | 已改 32 根≈8 天 |
| B5 | S10 跌停信号混除权机械跌幅（假跌停） | 疑似中 | **实测排除**：全宇宙 8 年 31960 个跌停信号中仅 78 个（0.24%）落在除权日，且前复权链本身已修正分红跳空 → 影响可忽略，记录不处理 |
| B6 | m60 新浪 60 分钟线为**不复权**价格，除权日产生假跳空 | 中 | 实锤（Sina getKLineData 无复权参数）→ 记录为口径限制：m60 研究遇除权日需剔除或用日K复权因子后修。暂不做（影响范围=2年内除权日，稀疏） |
| B7 | claims_registry 中 LIMITDOWN 的 note 过期（欠账已销但未更新） | 微 | 已清理 |

## B2 修正的连锁后果（最重要产出）

滚动窗口语义修正 + 退市股入库 + 数据刷新至 9/11，三者叠加后审计重跑：

| 主张 | 修正前 | 修正后（250 交易日历窗口，含退市股） |
|---|---|---|
| 低位避雷针 | DECAYING | **DEAD**（T+5 滚动 -0.92pp，T+20 -1.51pp） |
| 低位大长腿 | DECAYING | **DEAD**（T+5 -0.69pp，T+20 -0.22pp） |
| TD9买入 | DECAYING | **DEAD**（T+5 -0.44pp，T+20 -1.20pp） |
| 高位避雷针（黑名单） | CANDIDATE | CANDIDATE ✅（滚动 -2.71pp，维持有效否决信号） |

**昨晚报告的"形态×位置"规律在最近一年（含 9/11 前）数据里是死的。** 当前注册表活着的看多主张只剩两条：反转族（external 待复审）和跌停次日接（L2+，本轮双修复审通过）。这是回路自建成以来最大的实质性产出——它杀死的是我自己 12 小时前发布的结论。

## 方法论收获

1. **实证优先于推理**：最严重的嫌疑（B1 接缝）被实验证明不存在；中等嫌疑（B2）被实验证明致命。不跑实验靠读代码会得出相反的优先级。
2. **bug 修复必须触发重跑**：B2 不是"代码修了就完"，修正口径改变了结论状态——审计回路的正确性本身就是一级资产。

## 八、第三波自查（m60 回填期三件产物的审计，用户要求"大厂方法论"）

| # | 嫌疑 | 判决 |
|---|---|---|
| C1 | **HiThink 对账的日期标签可能是错的** | **实锤——我自己的结论错了**：官方池 date_ms=1789123200000 返回的 40 只与自家 9/11 涨停交集 38/40、与 9/10 仅 7/40 → 那是 9/11 的池子，我之前说"9/10 对账 40 vs 39"张冠李戴。正确对账：9/11 官方 40 vs 自家 39（差异=1只20cm创业板+1只001开头）。实证映射规则：**date_ms = (交易日−1天) 的 UTC 零点毫秒戳**（1789036800000→9/10池 34 只，金安国纪 9/10 +10.0% 涨停交叉验证）。"最新日池子次日才就绪"结论一并作废——9/11 池子当时就有，是我拿 9/12（非交易日）的时间戳查了个空。教训：对第三方 API 的语义假设必须用内容交叉验证，不能看 total 对上了就收工 |
| C2 | claims_shadow 一字剔除放在信号日 | 实锤 bug：可成交性只能在入场日判。已改：注册全量，入场回填时判一字涨停买不进/一字跌停锁死并标 untradeable |
| C3 | SHADOW_DATE 回填历史日时 detect 仍取最后一根 | 实锤 bug：旧代码会给已退市股票的 2019 年末日贴上 2026-09-11 的标签（幽灵信号）。已改为按日期索引。重登记后 0 幽灵，1685 单（830反转+25跌停，构成合理） |
| C4 | cron 副本同步纪律 | 再犯一次（claims_shadow 修 bug 后需重拷）——已同步。cp 副本模式累计两次漂移，下次改 cron 直接调 engine 路径的提案入队 |

**第一波（B1-B7）+ 第三波（C1-C4）合计 11 项：实证排除 3、实锤修复 5、口径记录 3。**

## §九 晚班批次自查（2026-09-12 夜，「都搞」五件套 + m60 成交重测）

**实证检查（已过）：**
- E1 cap_hist 日期升序：200 抽 200 全有序 → seat_gene 的 cap_at 线性扫描前提成立。
- E2 frontrun 检测逻辑复算：全宇宙梯队重建 + 5 只票手工复算，V1 事件 13 个全部结构正确（涨幅≈10%、梯队 4-6、T1 分布合理）；首板间隔/一字剔除/doubler 口径一致。首次抽查 0 事件系抽查脚本自身梯队只建 40 只票所致（抽查脚本 bug，非 engine bug）。
- E3 zhaban 检测逻辑复算：30 只票 43 事件，R 组结构（高开 6-10%/回踩 3-6%/收 10%）与 mid 入场 T+1 收益（+2.65~+12.98%）与全量统计自洽。

**结构口径（已知、已标注、暂不改）：**
- S1 seat_gene n_events 含无 kcache 票（300/688，9686 行）与窗口尾部无 T+5 的事件——画像计数与收益统计口径不同，报告已分别标注。
- S2 seat_gene_gates 全事件落在 2023-2026 段（数据仅 1 年深），管线 L2_时间分段必然 ❌，已用窗内中点分段替代并在报告说明。
- S3 seat_taste_tracker 的 T5 统计对窗口尾部事件欠权（无 T+5 可算），fwd_t5 每调用重建日期索引（O(n²)，性能不 correctness）。
- S4 cron 27dd2482fa75 的 prompt 里文件名是 seat_taste_rolling.json，实际落盘 seat_gene_rolling.json——no_agent 模式 prompt 不执行，纯文案错位，下次 update 顺手改。
- S5 hot_money_item_net_value 字段语义未与第二数据源交叉验证（HiThink 官方口径）——席位净额方向若错，反向指标结论会翻转，列为待验。
- S6 zhaban_m60_fill 为 m60 不复权空间全自洽（信号+成交+出场同空间），与日K 前复权结果存在除权日附近轻微事件错位（B5 历史实测 0.24% 可忽略）。
- S7 frontrun/zhaban 位置匹配对照池含信号日自身——与 law_pipeline 仓库标准一致，非本批新引入。

**残留乐观（最重要，防自己明天忘）：**
- R1 frontrun close-entry = 打板成交口径，封死板 fill 率黑盒未定量（+1.79 上界 vs 次日追 -0.52 下界）。
- R2 zhaban_m60_fill 的 60 分钟粒度无法识别秒级触碰+薄队列 → 成交率仍系统性偏高，其输出是「乐观下界」而非真实成交率。

### 晚班自抓 bug（三个，全部实证确认+已修）
- **B-N1 北向账户名混淆（严重）**：`香港中央结算有限公司`（北向总账户）vs `香港中央结算(代理人)有限公司`（H股 nominee）——含糊匹配把中集集团等含 H 股公司的 59% H 股持仓误计为北向。修为精确等值匹配。修正后覆盖率 2095→2080，moves 分布 220/0/1875 → 1120/954/6。
- **B-N2 HOLD_NUM_CHANGE 字段多态（中等）**：同字段既返回标签（增持/减持/新进/不变）也返回股数（"59524193"/"-6419"），原分类器只认标签致减持=0 假阳性。修为 move_class 双解析。
- **B-Z1 炸板 m60 首版选择偏差（严重，用户"测bug"要求下发现）**：首版信号含「当日最低涨幅∈[3,8.5%]」的事后条件，但真实挂单无法预知当日最低——深炸票（跌破+3%）被剔除出测试集，而它们的挂单是必成交的。修为宽信号（昨涨停+今开≥5%）+浅炸/深炸分组报告，重跑中。
- **B-P1 pgrep 自匹配（工程）**：`pgrep -f <pattern>` 命中自身命令行致误判进程存活，本晚触发两次。纪律：检查进程用 `ps aux | grep -F | grep -v grep`。
- **B-P2 陈旧进程覆盖修复产物（工程）**：north_profile 旧进程（修复前启动）晚于修复版完成，用 buggy 聚合覆盖了已修 JSON，需二次重跑恢复。纪律：修补数据产物的代码前先杀旧进程，或产物文件名带代码版本。
